# NETORACLE - Backend Environment Configuration

# Server Settings
PROJECT_NAME="NETORACLE"
API_PREFIX="/api"
HOST="0.0.0.0"
PORT=8000
ENVIRONMENT="development"

# CORS Settings (comma-separated list of allowed origins)
CORS_ORIGINS="http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000"

# Future Database Configuration (MongoDB - Prepared for later phases)
# MONGODB_URI="mongodb://localhost:27017"
# MONGODB_DB_NAME="hex_hive_db"

